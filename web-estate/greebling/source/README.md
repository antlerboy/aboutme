Static Greebling microsite. `dist/domains.json` is the shared inventory for the clickable map, grouped URL directory, and campaign-address table. It covers the public web estate and specialist entrances, with publication destinations kept separate from internal knowledge systems.

Edit the inventory, then run `python scripts/build-directory.py`. The page renders every link as static HTML and works without JavaScript. `templates/index.html` preserves the existing Greebling artwork, controls, and selected writing. Keep the two selected essays; do not restore previously removed articles.

Campaign destinations express routing intent unless independently verified. Preserve explicit pending-domain notes and the working alternative links until native domain status confirms activation. Do not infer public access from a source repository’s visibility.
